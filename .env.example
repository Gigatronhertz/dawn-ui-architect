# Copy to .env and fill in values
# Vite exposes only VITE_* variables to the frontend bundle

# URL of the backend API (local dev: http://localhost:3000, production: your deployed backend)
VITE_API_URL=http://localhost:3000
